/*let num=window.prompt('how old are you ?')
age=Number(num) //Ty[e conversion to number
age +=1;
console.log(age,typeof age)
//-----------------------OR------------------------------------------//
let numb=Number(window.prompt('how old are you ?'))
 numb +=1
 console.log(numb,typeof numb)
//-----------------------OR------------------------------------------//
*/
a=10;
b=20;
c=30;

a=Number(a)
b=Number(b)
c=Number(c)

// Assuming variables a, b, and c are already defined somewhere in your code

console.log(`${a}\n${typeof a}`);
console.log(`${b}\n${typeof b}`);
console.log(`${c}\n${typeof c}`);